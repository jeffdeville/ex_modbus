machine:
  # post:
  #   - curl -fsSLO https://get.docker.com/builds/Linux/x86_64/docker-1.13.1.tgz && sudo tar --strip-components=1 -xvzf docker-1.13.1.tgz -C /usr/local/bin
  #   - sudo container=yes docker daemon: { background: true }
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  override:
    - docker info
    # - docker build --rm=false -t ex_modbus .
    # - docker build --rm=false -t modbusslave ./test/docker/

test:
  override:
    - docker-compose up
  post:
    - docker-compose kill
